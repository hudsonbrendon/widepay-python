language: python
python:
  - "3.7"

# command to install dependencies
install:
 - pip install pipenv
 - pipenv install --dev
 - "pipenv run pip install https://github.com/hudsonbrendon/widepay-python/archive/master.zip"

# command to run tests
script: make dev